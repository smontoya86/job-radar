# Slack
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/XXX/YYY/ZZZ

# Gmail
GMAIL_CREDENTIALS_FILE=credentials.json
GMAIL_TOKEN_FILE=token.json

# Adzuna (optional)
ADZUNA_APP_ID=your_id
ADZUNA_APP_KEY=your_key

# SerpApi (optional — Google Jobs aggregator, ~$50/mo)
SERPAPI_KEY=your_key

# JSearch / RapidAPI (optional — free tier: 500 req/mo)
JSEARCH_API_KEY=your_key

# Database
# SQLite (local development — used when running outside Docker)
DATABASE_URL=sqlite:///data/job_radar.db
# Docker uses PostgreSQL automatically (set in docker-compose.yml)
# For external PostgreSQL (e.g. Supabase for GitHub Actions):
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# Dashboard
DASHBOARD_PORT=8501

# Scheduler
JOB_CHECK_INTERVAL_MINUTES=30
EMAIL_CHECK_INTERVAL_MINUTES=15
